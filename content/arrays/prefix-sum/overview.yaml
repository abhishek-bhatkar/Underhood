title: Answer range sums with running totals
summary: Build cumulative totals once, then subtract two prefix boundaries to answer a range in constant time.
abstraction: The prefix array includes a leading zero so a range beginning at index zero needs no special case.
