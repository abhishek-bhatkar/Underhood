title: Find a pair in a sorted array
summary: Move a left and right pointer inward, discarding one impossible side after each comparison.
abstraction: This model assumes ascending input so the sum comparison identifies which pointer can move safely.
